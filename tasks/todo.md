# TODO

## F08 — ProductCard component

Steps:
1. Write failing tests: renders product name, tagline, vote count, upvote button, maker name
2. Create `src/components/ProductCard.tsx`
3. Pass all tests (green)
