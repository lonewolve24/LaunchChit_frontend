# TODO

## F13 — InlineError component

Steps:
1. Write failing tests: renders error message, associates with a field via id, not rendered when message is null
2. Create `src/components/InlineError.tsx`
3. Pass all tests (green)
